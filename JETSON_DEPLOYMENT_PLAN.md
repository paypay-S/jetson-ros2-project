# Jetson 実機デプロイ・最適化計画

強化学習モデル（PPO）を Jetson 実機で安定して動作させるための課題とその解決に向けた最適化プランです。

## 📋 解決すべき課題一覧

| カテゴリ | 課題内容 | 影響 | シミュレーションとの差異 |
| :--- | :--- | :--- | :--- |
| **1. 推論性能** | PyTorch 推論の重さ | 制御ループの遅延 (Jitter) | シミュレータはステップ同期だが、実機はリアルタイム |
| **2. 物理ギャップ** | LiDAR のノイズ | ステアリングの振動、誤作動 | シミュレータは理想的な距離データ |
| **3. 制御の平滑化** | 指令値の不連続性 | メカニズムへの負荷、スリップ | シミュレータ内の物理演算と実機タイヤの摩擦 |
| **4. 低速域の挙動** | モーターの追従性 | EXP-13 での低速指令が通らない可能性 | シミュレータは低速でも線形に動く |
| **5. 安全性** | 緊急停止の精度 | 壁への激突、回避不能な状況 | 実機では衝突が致命的な故障につながる |

---

## 🛠️ 最適化・開発ロードマップ

### ステップ 1: 推論の高速化（ONNX Runtime / TensorRT）
PyTorch モデルを ONNX 形式へ変換し、Jetson の性能を最大限に引き出します。
- [ ] PyTorch (`.zip`) モデルから ONNX 形式へのエクスポート
- [ ] `rl_driver.py` での ONNX Runtime 推論の実装
- [ ] 1 推論あたりのミリ秒単位での計測と目標設定

### ステップ 2: センサーデータのフィルタリング
LiDAR のノイズが AI の判断に与える影響を最小限にします。
- [ ] メディアンフィルタや移動平均による突発的ノイズの除去
- [ ] ダウンサンプリング、中心クロップの見直し
- [ ] `lidar_noise_std` パラメータの最適化

### ステップ 3: 指令値の平滑化と物理整合（Sim-to-Real）
タイヤのスリップやメカへの負担を軽減し、スムーズな走行を実現します。
- [ ] EMA（指数移動平均）フィルタの係数調整
- [ ] ステアリング角度の急激な変化（カクつき）の抑制
- [ ] モーターの不感帯（デッドバンド）対策

### ステップ 4: 実機安全レイヤーの強化
AEB (Autonomous Emergency Braking) を改良し、信頼性を高めます。
- [ ] 多角的な障害物検知アルゴリズムの導入
- [ ] 速度に応じた停止距離の動的な調整
- [ ] 外部スイッチ、手動オーバーライドの再確認

---

## 💡 今後の進め方
このプランに基づき、**ステップ 1（推論の高速化）** から着手することを推奨します。Jetson 上での制御ループの安定（低レイテンシ）は、全ての最適化の土台となるためです。
